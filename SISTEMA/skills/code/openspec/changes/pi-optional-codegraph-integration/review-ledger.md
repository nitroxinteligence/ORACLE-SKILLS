# Review Ledger

review_stage: post-apply
target_revision: sha256:22087f42def765edcb5d1b2ef2be93f75eba5a711e5ca962269e14f844883785

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R4-001 | resilience | `openspec/changes/pi-optional-codegraph-integration/apply-progress.md:269` | CRITICAL | verified | First-pass finding and refuter disposition are preserved in the post-apply record. The remediation RED/GREEN evidence records `TestPiCodeGraphFailsClosedForBrokenProjectMCPOverride` passing after effective project MCP precedence was resolved; the scoped re-review verified the effective resolver and fail-closed path. |
| R4-002 | resilience | `openspec/changes/pi-optional-codegraph-integration/apply-progress.md:270` | CRITICAL | verified | First-pass RED showed deselection produced no Pi snapshot targets. The recorded GREEN test includes the manifest and its owned overlay; the scoped re-review verified snapshot coverage and journal-before-mutation behavior. |
| R4-003 | resilience | `openspec/changes/pi-optional-codegraph-integration/apply-progress.md:271` | CRITICAL | verified | First-pass/refuter record led to the transactional uninstall fix. Recorded GREEN evidence restores MCP and overlay on cleanup failure and retains the manifest for retry; scoped re-review verified the recovery path. |
| R4-004 | resilience | `openspec/changes/pi-optional-codegraph-integration/apply-progress.md:271` | CRITICAL | verified | First-pass/refuter record led to the unreadable-child fail-closed fix. Recorded GREEN evidence rejects unreadable children before mutation and preserves the manifest; scoped re-review verified that behavior. |
| R4-005 | resilience | `openspec/changes/pi-optional-codegraph-integration/apply-progress.md:272` | CRITICAL | verified | First-pass RED showed journal restore errors were ignored. Recorded GREEN evidence returns both the triggering failure and `restore Pi CodeGraph journal`; scoped re-review verified error joining for reconcile and uninstall. |
| R4-006 | resilience | `openspec/changes/pi-optional-codegraph-integration/apply-progress.md:273` | CRITICAL | verified | First-pass RED showed an unmatched managed marker truncated the child. Recorded GREEN evidence fails reconciliation and keeps the child byte-identical; scoped re-review verified the fail-closed renderer behavior. |
| R4-007 | resilience | `openspec/changes/pi-optional-codegraph-integration/reviews/post-apply/review-ledger.md:15` | WARNING | info | First-pass warning: stale package overlays after sync are informational only. It did not enter refutation, fix, or scoped re-review, and no change was authorized. |
| R4-008 | resilience | `internal/agents/pi/adapter.go`, `internal/components/communitytool/pi_codegraph.go` | CRITICAL | fixed | Project-local effective MCP configuration is now decoupled from the adapter directory. `TestCodeGraphPathsKeepsAgentDirectoryWhenProjectMCPOverrides` proves `$PI_CODING_AGENT_DIR` remains the adapter home while `.mcp.json` is effective; the reconciliation probe passes the actual agent directory for project-local MCP paths. |
| R4-009 | resilience | `internal/cli/run.go` | CRITICAL | fixed | `piCodeGraphReconcileStep` is a rollback step and invokes manifest-scoped removal for dynamic Pi overlays created after package installation. `TestPiCodeGraphReconcileStepRollbackRemovesDynamicPackageOverlay` proves a later pipeline rollback removes the dynamically discovered overlay. |
| R4-010 | resilience | `internal/cli/run.go`, `internal/app/app.go`, `internal/tui/model.go`, `internal/tui/screens/complete.go` | CRITICAL | fixed | Pi CodeGraph `ManualActions` now propagate through CLI and TUI completion rendering. `TestRenderInstallManualActionsIncludesPiCodeGraphDrift`, `TestCompleteViewShowsPipelineManualActions`, and `TestRenderCompleteShowsManualActions` prove actionable drift output. |
| R4-011 | resilience | `internal/components/communitytool/pi_codegraph.go:523-568` | CRITICAL | fixed | The Pi runtime probe now parses exactly one `codegraph: <state>` capability record and accepts only an exact healthy state. `TestPiCodeGraphAdapterRuntimeFailsClosedWithoutPositiveCapabilityEvidence` proves `not ready`, `not running`, `unloaded`, `inactive`, `broken`, malformed, unrelated, and duplicate/ambiguous output fail closed; `connected` and `ready` pass. |

## Review Evidence Scope

This is the canonical post-apply ledger normalized from the existing stage-scoped ledger and `apply-progress.md`. Rows R4-001 through R4-006 and R4-008 through R4-010 are verified. R4-007 remains informational. R4-011 is fixed.
