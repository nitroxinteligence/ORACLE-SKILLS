# Review Ledger — engram-protocol-dedup

Round 1 — judgment-day (jd-judge-a + jd-judge-b, blind parallel) over the DESIGN artifacts (design.md, spec, proposal) validated against the codebase. Pre-implementation review.

| id | lens | severity | file:line | status | summary |
|----|------|----------|-----------|--------|---------|
| JD-001 | judgment-day | CRITICAL | design.md:74 (vs internal/cli/run.go:1123-1141) | verified | design.md Decision 4 now names a real seam: `ProbeProtocolFlag(ctx)` in `internal/components/engram/setup.go`, following `VerifyVersion`'s `execCommand(...).Output()` pattern, with `exec.CommandContext` 5s deadline + detached stdin. File Changes, Testing Strategy, and the sequence diagram updated to match. |
| JD-002 | judgment-day | CRITICAL | design.md:10,18-20,89 (vs internal/cli/run.go:794) | verified | Researched `/home/gentleman/work/engram` git history (read-only): `server.WithInstructions(serverInstructions)` shipped in commit 281bb1e, first released in tag v1.4.0 (not in v1.3.1). Design now gates Claude-slim on `engram --version` ≥ v1.4.0 via the existing `VerifyVersion`; below-floor/unknown → full. Decision 1, TL;DR, Decision 4, and proposal.md Risks updated. |
| JD-003 | judgment-day | CRITICAL | design.md:162,166 (vs testdata/golden/) | verified | Corrected golden numbers with verified counts: of the 7 `engram-*.golden` fixtures, exactly 1 changes (`engram-claude-claudemd.golden`), 6 stay byte-stable; only 2 of 7 contain protocol prose (`engram-claude-claudemd.golden`, `engram-antigravity-rulesmd.golden`), the other 5 are MCP/settings-config-only JSON. `combined-claude-claudemd.golden` documented as a separate fixture family (not matched by `engram-*` glob) that also regenerates. |
| JD-004 | judgment-day | WARNING | design.md:59,72 (vs internal/assets/{claude,codex} assets) | verified | Corrected bullet counts (verified by count): Claude's PROACTIVE SAVE TRIGGERS = 12 bullets (not 11), Codex's WHEN TO SAVE = 6 bullets; rendered Codex output gains ~6 additional bullets (not ~5) plus the self-check line. Also fixed "102-line" → "101-line" full asset (wc -l verified). |
| JD-005 | judgment-day | WARNING | design.md:106 (vs internal/components/engram/setup.go:40-56) | verified | Removed fabricated claim that OpenCode and Kilocode share a slug — `SetupAgentSlug` maps them to distinct slugs (`opencode`, `kilocode`). Kept gemini-cli (Gemini CLI + Antigravity) as the only real shared-slug example; attemptedSlugs dedup + safest-wins rule left unchanged. |
| JD-006 | judgment-day | WARNING | proposal.md:69 (vs design.md:57) | verified | proposal.md Success Criteria now states ~1,045 net tokens/session saved (1,175 gross − 130 slim), matching design.md Decision 2. |
| JD-007 | judgment-day | WARNING | design.md:65 | verified | Decision 3 marker table now shows paired open/close markers for all four sections and states `codexInstructions()` = full + passive-capture concatenation. |
| JD-008 | judgment-day | WARNING | proposal.md:46 | verified | Path corrected: `internal/testdata/golden/` → `testdata/golden/` in Affected Areas table. |
| JD-009 | judgment-day | SUGGESTION | proposal.md:5,68; design.md:35 | verified | proposal.md Success Criteria reworded to "single maintained source" (not fewer per-session copies); design.md Decision 1 Pi row relabeled from "already slim" to "no protocol text (MCP-only precedent)". |
Round 2 — scoped re-judge: JD-001/006/007/008/009 verified; JD-003/004/005 REOPENED (fix agent introduced factual errors); new findings JD-010/011.

| JD-010 | judgment-day | CRITICAL | design.md:18,148,162 (vs internal/cli/run.go:1587, inject.go InjectOptions) | verified | Removed "no new probe needed" claim; documented required plumbing: `VerifyVersion()` is only called inside a soft health check that discards the version string, `InjectOptions` has no version field. Added File Changes rows threading `VerifyVersion()` into `InjectOptions.Version`/`MinVersionMet` plus an exported test seam, and a matching Testing Strategy row (below-floor/at-floor/unknown → full). |
| JD-011 | judgment-day | WARNING | design.md Testing Strategy | verified | Added `combined-windsurf-global-rules.golden` to Testing Strategy as an explicit "stays unchanged" fixture (embeds full protocol section, but Windsurf remains full). |
Round 3 — scoped re-judge: JD-003/004/005/011 verified by both judges; JD-010 verified after an orchestrator-applied boundary fix (judge A round-3 finding: Testing Strategy row said "at-floor -> full" contradicting the inclusive >= v1.4.0 floor; corrected to explicit at-floor-permits-slim boundary assertion, and the InjectOptions.Version field-name hedge removed). Terminal state: JUDGMENT APPROVED.

Implementation round 1 — judgment-day over the uncommitted apply diff (parallel with sdd-verify, which passed 0-critical).

| JD-012 | judgment-day | CRITICAL | internal/components/engram/setup.go:24-45 | verified | Replaced hand-rolled goroutine/select cancellation with `execCommandContext(ctx, ...)` (new `exec.CommandContext`-backed seam) — stdlib now owns kill-on-cancel synchronization. RED: `setup_race_test.go` `TestRunProtocolProbeCommandRealProcessDeadline` reproduced the data race (real `go test -race` failure) against the old code via a command-override substituting a real `sleep 10` process; GREEN after the swap, verified `-race -count=5`. `apply-progress.md` Deviations updated to disclose the original hand-rolled version honestly. |
| JD-013 | judgment-day | WARNING | internal/cli/run.go (willAttemptSetup gate, ~:901-920) | verified | Probe now gated on `willAttemptSetup` (≥1 adapter passes `ShouldAttemptSetup(setupMode, agent)`); `verifyEngramVersion` stays unconditional. RED/GREEN: `TestRunInstallSkipsProtocolProbeWhenSetupModeOff` (protocol_forwarding_test.go) — 0 probe calls under `GENTLE_AI_ENGRAM_SETUP_MODE=off`. |
| JD-014 | judgment-day | WARNING | internal/components/engram/protocol.go:99-124 | verified | `engramVersionPattern` anchored to `^(?:engram\s+)?v?(\d+)\.(\d+)\.(\d+)$` on the trimmed string — accepts `X.Y.Z`/`vX.Y.Z`/`engram X.Y.Z`, rejects pre-release suffixes and embedded/trailing garbage (falls back to full). RED/GREEN: `protocol_version_floor_test.go` `TestEngramVersionMeetsFloor`, 14 table cases. |
| JD-015 | judgment-day | WARNING | internal/assets/assets_test.go:474-506 | verified | Moved the `claude/commands` (10) and `claude/agents` (17) count assertions into `TestClaudeEmbeddedAssetLayout`; `TestEngramEmbeddedAssetLayout` now scoped to `engram/` only. |
| JD-016 | judgment-day | SUGGESTION | internal/cli/run.go (componentApplyStep.Run version cache ~:894-899; engramHealthChecks ~:1636) | verified | `componentApplyStep.Run` caches the resolved version/error on `runtimeState`; `engramHealthChecks` (now takes `*runtimeState`) reuses the cache, falling back to the `verifyEngramVersion` seam (not a direct `engram.VerifyVersion()` call) when unresolved. RED/GREEN: `TestRunInstallShellsOutEngramVersionOnlyOnce` using new `engram.CountVersionCallsForTest` — count 2 -> 1. |
| JD-017 | judgment-day | SUGGESTION | internal/cli/run.go (computeSlugSlimVerdicts ~:731-748) + protocol_slug_verdict_test.go | verified | Extracted `computeSlugSlimVerdicts(agentIDs, isSlim)` (run.go) as a pure, injectable-verdict helper. New `protocol_slug_verdict_test.go` pins safest-wins AND semantics with a fabricated slim/full split across the real shared `gemini-cli` slug (Gemini CLI + Antigravity), both orders, plus both-true/both-false. |
| JD-018 | judgment-day | SUGGESTION | internal/components/engram/protocol.go:75-77; design.md | verified | Added an ordering note to design.md Decision 3: PASSIVE CAPTURE intentionally renders after AFTER COMPACTION (concatenation order), differing from the original asset's interleaving; content byte-preserved. |
| JD-019 | judgment-day | SUGGESTION | internal/components/engram/{verify,setup}.go | verified | Added invariant-pinning comments above `runVersionCommand` (verify.go) and `runProtocolProbeCommand` (setup.go) documenting that install branches must keep process PATH pointing at the intended engram binary before these run. Comment-only. |

Implementation round 2 — scoped re-judge: JD-012..JD-019 all RESOLVED by both judges (judge B: CLEAN; judge A: functional-clean with doc/hygiene residuals). go test -race full-repo clean. Terminal state: JUDGMENT APPROVED (implementation).

| JD-020 | judgment-day | SUGGESTION | internal/components/engram/setup_race_test.go:28-32 | info | Vestigial execCommand override in the race test no longer exercises anything (runProtocolProbeCommand only uses execCommandContext); remove or annotate as historical RED context. Both judges, round 2. |
| JD-021 | judgment-day | SUGGESTION | internal/cli/run.go:441 | info | runtimeState.engramVersion field written once, never read (only engramVersionResolved/Err consumed); drop or use in the health-check description. Judge A, round 2. |
| JD-022 | judgment-day | SUGGESTION | internal/components/engram/setup_race_test.go:29-36 | info | Race test spawns a real `sleep 10`, not natively available on Windows; CI is ubuntu-only so no break, but a portability note for contributors testing on native Windows. Judge A, round 2. |
